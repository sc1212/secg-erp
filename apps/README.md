# Apps

Target monorepo application homes:
- `apps/api`: future TypeScript backend (NestJS) migration target
- `apps/web`: future Next.js frontend

Current production backend remains in `backend/` and is the active runtime until migration slices are completed.
