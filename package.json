{
  "name": "secg-erp",
  "private": true,
  "version": "0.1.0",
  "description": "SECG ERP monorepo workspace scaffold",
  "packageManager": "pnpm@9.12.2",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck"
  },
  "devDependencies": {
    "turbo": "^2.1.3"
  }
}
