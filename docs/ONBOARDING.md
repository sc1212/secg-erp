# ONBOARDING

## Current backend (existing)
1. `docker compose up -d`
2. `bash setup.sh`
3. Open `http://localhost:8000/api/docs`

## Future monorepo workflow (target)
1. `pnpm install`
2. `pnpm dev`


## Canonical repo
- Active development repository: `sc1212/erp`.
- Avoid committing active feature work to `sc1212/SECG1` to prevent drift.
